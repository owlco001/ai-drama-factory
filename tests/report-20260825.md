# core-engine MVP验收测试报告

- 日期：2026-08-25
- 执行人：ai_test（测试Agent）
- 被测对象：「Agent团队」core-engine（代码复审"有条件通过"后，条件项P2不阻塞）
- 结论：**通过MVP验收（26/26用例全绿，无阻塞问题）**

---

## 1. 测试环境与执行方式

| 项 | 内容 |
|---|---|
| JDK | OpenJDK 17.0.19 (Debian) |
| Gradle | Gradle 8.10（/tmp/gradle-8.10，项目无gradle wrapper，使用系统级Gradle） |
| Kotlin | 2.0.21 |
| 环境 | Linux容器，无Android SDK/真机——仅core-engine纯Kotlin模块可测 |

### 1.1 现有单测执行
命令：`cd src && gradle :core-engine:test --rerun-tasks`
结果：BUILD SUCCESSFUL in 5s，4 tasks executed。

### 1.2 新增黑盒验收测试
新增独立Gradle工程 `/root/project_workspace/tests/`（不改动src/任何源码），以 `files(../src/core-engine/build/libs/core-engine.jar)` 引用被测产物：
- `tests/src/test/kotlin/acceptance/AcceptanceTest.kt`（4条黑盒用例）
- `tests/build.gradle.kts` / `tests/settings.gradle.kts`
命令：`cd tests && gradle test --rerun-tasks` → BUILD SUCCESSFUL。
HTML报告：`tests/build/reports/acceptance/index.html`

## 2. 用例统计

| 套件 | 总数 | 通过 | 失败 | 阻塞 |
|---|---|---|---|---|
| OrchestratorTest | 5 | 5 | 0 | 0 |
| CoreModulesTest | 7 | 7 | 0 | 0 |
| Round2FixTest | 10 | 10 | 0 | 0 |
| AcceptanceTest（新增黑盒） | 4 | 4 | 0 | 0 |
| **合计** | **26** | **26** | **0** | **0** |

失败项完整报错上下文：无失败项。（过程中一次B用例误判已修正：@点名任务为单节点直达，不应按多Agent编排成败断言；修正后通过。）

### 新增黑盒用例明细
| 编号 | 用例 | 验证点 | 结果 |
|---|---|---|---|
| A | 核心链路端到端 | 提问→DAG≥3节点→各Agent串行SUCCESS→FINAL_OUTPUT返回user | PASS |
| B | @点名直达 | "@校验 复核上一段"解析为verifier+正确instruction；"@分析"解析为analysis；普通输入不解析 | PASS |
| C | 工具白名单拒绝 | creation调用白名单外工具 rm_rf → ok=false 拒绝 | PASS |
| D | 结构化JSON消息 | 总线消息含 msgId/from/to/payload(JsonObject)/taskId | PASS |

## 3. PRD §8 成功指标逐条核验

| # | 指标 | 核验方式 | 状态 |
|---|---|---|---|
| 8.1 | 核心链路（提问→DAG展示→≥3个Agent串行完成→结果返回）端到端成功率 ≥90% | 自动化证明：OrchestratorTest「正常输入产出FINAL_OUTPUT且四节点全部SUCCESS」+ Round2FixTest「P0-1 上游结果经context_refs传递到下游」+ 黑盒A/D。当前样本100%（受限于FakeEngine确定性桩，非统计意义成功率） | ✅ 达标（自动化证明存在） |
| 8.2 | 8GB机型连续30分钟无OOM崩溃 | 无法核验：需真机+真实GGUF模型；JNI桩未接入native库 | ⛔ 未核验（真机冒烟阶段项） |
| 8.3 | 用户可在面板/日志/DAG中追溯任意一条Agent消息 | 部分自动化：bus.history(taskId)按task可完整追溯全部消息（CoreModulesTest + D用例）；UI面板展示属app壳层，本层不可测 | ✅ 数据层达标 / UI待集成验证 |

## 4. 其他静态可验项

| 项 | 要求 | 实测 | 状态 |
|---|---|---|---|
| 包体积（不含模型） | ≤100MB | core-engine.jar = 124KB；依赖仅coroutines+serialization，零网络库（grep无OkHttp/Retrofit/Http引用） | ✅ |
| 内存约束 | ≤2.5GB含模型 | 本层不可测（模型/JNI未接入）；InMemoryMemoryStore有L1窗口裁剪逻辑及单测 | ➖ 待真机 |
| 串行调度开销 | ≤200ms/往返 | FakeEngine下全链路<20ms，真实开销取决于推理引擎 | ➖ 待真机 |
| 单Agent异常不崩溃会话 | 可重试、标FAILED、后继SKIPPED | 已覆盖：失败重试、环DAG拒绝、CancellationException传播等10条Round2回归 | ✅ |
| 星型拓扑约束 | 子Agent间禁止直连 | TopologyValidator两条用例通过 | ✅ |
| @点名语法 | 以@开头即点名 | 通过（B用例 + OrchestratorTest） | ✅ |

## 5. 阻塞问题清单

**无阻塞问题。**

遗留观察项（不阻塞MVP验收）：
1. LlamaJni为编译桩（loadModel返回false降级MODEL_ERROR），真实libllama-android.so接入后需重跑核心链路；
2. §8.1的"90%成功率"为统计指标，建议真机冒烟阶段用≥50条多样化输入统计验证；
3. 三层记忆L3持久化目前为内存实现接口，落盘策略按PRD Q1待产品确认。

---
*附：单测XML结果 `src/core-engine/build/test-results/test/*.xml`；验收HTML报告 `tests/build/reports/acceptance/index.html`*
