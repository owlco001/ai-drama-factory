# 「AI短剧工厂」决议记录

> 2026-08-25 老王确认（"继续"=全盘采纳产品Agent建议）。与 PRD §8 待确认问题对应，冲突处以本文件为准。

| # | 决议 |
|---|---|
| Q1 | 开发前先做技术冒烟：模拟移动网络环境实测 POST /videos 直连 Agnes；失败再加轻量代理 |
| Q2 | MVP 只做条数型预算上限；金额按平台牌价仅展示估算 |
| Q3 | 第三方供应商（可灵/即梦）P2 再选型；MVP 只保证 VideoProvider 适配器接口抽象合理 |
| Q4 | 用户协议 + 导入小说时勾选版权确认框 |
| Q5 | 冒烟测试 FFmpegKit 端侧拼接 24 镜；性能不达标降级为分段导出+云端组装 |
| Q6 | 文本/图像/视频三通道独立适配器架构；MVP 共用一个 Key |
| Q7 | 设置页提供各国产ROM后台保活指引 |
| Q8 | 多人协作评审列 P2 云同步范畴 |
| Q9 | 默认注入中文配音指令 prompt（继承 pavo-drama 实战方案：中文台词开头主导+显式「全程中文普通话配音」指令），作为产品默认行为 |

## 技术冒烟前置任务（架构阶段第一项）
- S1: 本机直连 Agnes POST /videos 提交1镜测试任务并轮询至完成（验证Q1）
- S2: FFmpegKit 拼接 24 段 448x832 MP4 性能基准（验证Q5，可在本机用 ffmpeg 模拟）

---

## 技术冒烟结果（2026-08-25 实测）

### S1: Agnes 直连 ✅ 通过
- POST /videos 提交成功（video_id 正常返回，status=queued）
- GET /agnesapi?video_id= 轮询正常（in_progress/progress=30）
- 结论：Q1 解除——移动端直连方案可行，无需代理层

### S2: FFmpeg 端侧拼接 ⚠️ 有条件通过
- 同规格片段 `-c copy` 拼接：24镜瞬时完成（本机2.5s含生成）
- 混合分辨率归一化重编码（scale+pad+libx264）：本机11.1s
- 结论：Q5 初步解除——端侧拼接可行；但服务器CPU远强于手机SoC，
  安卓端需用 FFmpegKit + 硬件编码器(h264_mediacodec)实测，
  保留"分段导出"降级路径。真机冒烟列入开发阶段验收项。
