# T002 AI助手流式回复与自然语言优化

目标：
1. 优化 AI 助手的自然语言能力（提示词优化，使其更像对话助手）。
2. 将 AI 助手的回复改为流式输出（Streaming），边生成边展示。

要求：
- 检查 `TextProvider` 接口是否支持流式返回（如 `Flow<String>`）。
- 梳理目前 `AiAgent` 或 `TextModelRouter` 相关实现，增加/对接流式 chat 接口。
- 修改 Compose UI（如对话框或相关页面）以支持收集并显示流式打字机效果。
- 保证向后兼容，不破坏原本的非流式短小请求。
